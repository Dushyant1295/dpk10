{% extends "base.twig" %}
{% block content %}

<section class="press-detail-page">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1>{{ post.title }}</h1>
                {{ post.content }}
            </div>
        </div>
    </div>
</section>

{% endblock %}